
Router.route('/', function () {
  this.render('landing', {
    
  });
});

Router.route('/system-admin', function () {
  this.render('systemAdmin', {
    
  });
});


Router.route('/system-sites', function () {
  this.render('systemAdmin', {
    
  });
});